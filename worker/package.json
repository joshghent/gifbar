{
  "name": "gifbar-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies GIPHY and Tenor so their API keys never ship inside the desktop app",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "~4.13.5"
  },
  "devDependencies": {
    "vitest": "~4.1.11",
    "wrangler": "^4.124.0"
  }
}
